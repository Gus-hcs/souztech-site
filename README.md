# souztech.com

Site de apresentacao do Souz Controle de Obra. Uma pagina estatica servida pelo GitHub Pages.

- `index.html` — a pagina (mesma coisa que `docs/landing.html` no repo do produto).
- `CNAME` — o dominio.

Para publicar: GitHub Pages -> Deploy from a branch -> `main` / `(root)`.
